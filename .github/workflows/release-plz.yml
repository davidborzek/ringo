name: Release-plz

# release-plz owns versioning, changelogs, crates.io publishing (in dependency
# order) and GitHub releases for the whole workspace. The binary build (ringo-phone)
# and the Docker image (ringo-flow) hang off release-plz's `releases` output in the
# same run, so no cross-workflow tag trigger / PAT is needed.
on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  # Opens/updates the "Release PR" with version bumps + changelogs.
  release-pr:
    name: Release PR
    runs-on: ubuntu-latest
    concurrency:
      group: release-plz-pr
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          submodules: true
      - uses: dtolnay/rust-toolchain@stable
      - name: Install build dependencies
        # The publish verify-build links the ringo-phone/ringo-flow binaries
        # against opus + spandsp (dynamic, like the release binaries / Homebrew).
        # Retries: a CDN reset mid-download would otherwise fail the step.
        run: |
          echo 'Acquire::Retries "5";' | sudo tee /etc/apt/apt.conf.d/80-retries
          sudo apt-get update
          sudo apt-get install -y libopus-dev libspandsp-dev libpulse-dev pkg-config
      - uses: MarcoIeni/release-plz-action@v0.5
        with:
          command: release-pr
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

  # On merge of the Release PR: publish to crates.io (core → phone/flow), tag, and
  # create GitHub releases. Exposes which crates were released (+ their tags).
  release:
    name: Release
    runs-on: ubuntu-latest
    outputs:
      phone_tag: ${{ steps.tags.outputs.phone_tag }}
      flow_tag: ${{ steps.tags.outputs.flow_tag }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          submodules: true
      - uses: dtolnay/rust-toolchain@stable
      - name: Install build dependencies
        # The publish verify-build links the ringo-phone/ringo-flow binaries
        # against opus + spandsp (dynamic, like the release binaries / Homebrew).
        # Retries: a CDN reset mid-download would otherwise fail the step.
        run: |
          echo 'Acquire::Retries "5";' | sudo tee /etc/apt/apt.conf.d/80-retries
          sudo apt-get update
          sudo apt-get install -y libopus-dev libspandsp-dev libpulse-dev pkg-config
      - name: Run release-plz
        id: release-plz
        uses: MarcoIeni/release-plz-action@v0.5
        with:
          command: release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
      - name: Extract released tags
        id: tags
        if: steps.release-plz.outputs.releases_created == 'true'
        run: |
          rel='${{ steps.release-plz.outputs.releases }}'
          echo "phone_tag=$(echo "$rel" | jq -r '.[] | select(.package_name=="ringo-phone") | .tag // empty')" >> "$GITHUB_OUTPUT"
          echo "flow_tag=$(echo "$rel" | jq -r '.[] | select(.package_name=="ringo-flow") | .tag // empty')" >> "$GITHUB_OUTPUT"
          echo "mcp_tag=$(echo "$rel" | jq -r '.[] | select(.package_name=="ringo-mcp") | .tag // empty')" >> "$GITHUB_OUTPUT"

  # ringo-phone binaries → attached to its GitHub release (only if released).
  # The cross-platform build/link/smoke logic is shared with PR CI via the
  # reusable build-binaries workflow (upload=true here attaches the tarballs).
  phone-binaries:
    needs: release
    if: needs.release.outputs.phone_tag != ''
    permissions:
      contents: write
    uses: ./.github/workflows/build-binaries.yml
    with:
      package: ringo-phone
      ref: ${{ needs.release.outputs.phone_tag }}
      tag: ${{ needs.release.outputs.phone_tag }}
      upload: true

  # ringo-flow binaries → attached to its GitHub release (only if released).
  flow-binaries:
    needs: release
    if: needs.release.outputs.flow_tag != ''
    permissions:
      contents: write
    uses: ./.github/workflows/build-binaries.yml
    with:
      package: ringo-flow
      ref: ${{ needs.release.outputs.flow_tag }}
      tag: ${{ needs.release.outputs.flow_tag }}
      upload: true

  # ringo-mcp binaries → attached to its GitHub release (only if released).
  mcp-binaries:
    needs: release
    if: needs.release.outputs.mcp_tag != ''
    permissions:
      contents: write
    uses: ./.github/workflows/build-binaries.yml
    with:
      package: ringo-mcp
      ref: ${{ needs.release.outputs.mcp_tag }}
      tag: ${{ needs.release.outputs.mcp_tag }}
      upload: true

  # ringo-flow Docker image → GHCR (only if ringo-flow was released).
  flow-docker:
    needs: release
    if: needs.release.outputs.flow_tag != ''
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/docker-image.yml
    with:
      ref: ${{ needs.release.outputs.flow_tag }}
      flow_tag: ${{ needs.release.outputs.flow_tag }}

  # Homebrew: render the formula templates with the release binaries' checksums
  # and push them to the tap. Runs after the binaries are attached.
  # Runs if either crate was released; one of the binary jobs may be skipped (only
  # the released crate builds), so guard with !cancelled() && !failure() rather than
  # the default all-needs-succeeded. homebrew.yml renders only the formula(e) whose
  # tag is non-empty.
  homebrew:
    needs: [release, phone-binaries, flow-binaries, mcp-binaries]
    if: ${{ !cancelled() && !failure() && (needs.release.outputs.phone_tag != '' || needs.release.outputs.flow_tag != '' || needs.release.outputs.mcp_tag != '') }}
    uses: ./.github/workflows/homebrew.yml
    secrets: inherit
    with:
      phone_tag: ${{ needs.release.outputs.phone_tag }}
      flow_tag: ${{ needs.release.outputs.flow_tag }}
      mcp_tag: ${{ needs.release.outputs.mcp_tag }}

  # Documentation site → GitHub Pages, built from the released tag's tree so the
  # live site matches the latest release. Called from here rather than triggered by
  # the tag push: release-plz tags with the default GITHUB_TOKEN, and GitHub does not
  # fire workflows for events that token creates — same reason the binary/Docker jobs
  # hang off `releases` instead of a tag trigger.
  docs:
    needs: release
    if: ${{ needs.release.outputs.phone_tag != '' || needs.release.outputs.flow_tag != '' || needs.release.outputs.mcp_tag != '' }}
    permissions:
      contents: read
      pages: write
      id-token: write
    uses: ./.github/workflows/docs.yml
    with:
      # Both crates are tagged on the same release commit, so either tag names the
      # same tree; prefer flow's, fall back to phone's when only phone was released.
      ref: ${{ needs.release.outputs.flow_tag != '' && needs.release.outputs.flow_tag || needs.release.outputs.mcp_tag != '' && needs.release.outputs.mcp_tag || needs.release.outputs.phone_tag }}

  # Pin the released store paths in the Nix binary cache, so the versions people
  # pin outlive the cache's least-recently-used eviction. Needs only the tags —
  # nix-cache.yml has already built the same tree from main.
  nix-pin:
    needs: release
    if: ${{ needs.release.outputs.phone_tag != '' || needs.release.outputs.flow_tag != '' || needs.release.outputs.mcp_tag != '' }}
    uses: ./.github/workflows/nix-pin.yml
    secrets: inherit
    with:
      phone_tag: ${{ needs.release.outputs.phone_tag }}
      flow_tag: ${{ needs.release.outputs.flow_tag }}
      mcp_tag: ${{ needs.release.outputs.mcp_tag }}

  # AUR `-bin` packages — same gating as homebrew; aur.yml publishes only the
  # package(s) whose tag is non-empty.
  aur:
    needs: [release, phone-binaries, flow-binaries, mcp-binaries]
    if: ${{ !cancelled() && !failure() && (needs.release.outputs.phone_tag != '' || needs.release.outputs.flow_tag != '' || needs.release.outputs.mcp_tag != '') }}
    uses: ./.github/workflows/aur.yml
    secrets: inherit
    with:
      phone_tag: ${{ needs.release.outputs.phone_tag }}
      flow_tag: ${{ needs.release.outputs.flow_tag }}
      mcp_tag: ${{ needs.release.outputs.mcp_tag }}
