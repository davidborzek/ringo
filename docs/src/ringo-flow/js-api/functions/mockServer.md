# Function: mockServer()

> **mockServer**(`opts?`): [`MockServer`](../interfaces/MockServer.md)

Start a mock HTTP server (`mockServer({ port })`); a free port is chosen if none.

## Parameters

### opts?

#### port?

`number`

## Returns

[`MockServer`](../interfaces/MockServer.md)
