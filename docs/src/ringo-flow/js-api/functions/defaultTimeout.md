# Function: defaultTimeout()

> **defaultTimeout**(`duration`): `void`

Set the default `until` timeout for the rest of the script (e.g. `"10s"`).

## Parameters

### duration

`string`

## Returns

`void`
