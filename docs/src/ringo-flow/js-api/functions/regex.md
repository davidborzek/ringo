# Function: regex()

> **regex**(`pattern`): [`PathMatch`](../interfaces/PathMatch.md)

A regex path matcher for the mock server's respond/requestCount/lastRequest/requests.

## Parameters

### pattern

`string`

## Returns

[`PathMatch`](../interfaces/PathMatch.md)
