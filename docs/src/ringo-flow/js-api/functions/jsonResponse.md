# Function: jsonResponse()

> **jsonResponse**(`body`, `status?`): [`MockResponseSpec`](../interfaces/MockResponseSpec.md)

A `application/json` response spec (body JSON-encoded) for `respond`.

## Parameters

### body

`any`

### status?

`number`

## Returns

[`MockResponseSpec`](../interfaces/MockResponseSpec.md)
