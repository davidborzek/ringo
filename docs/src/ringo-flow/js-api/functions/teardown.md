# Function: teardown()

> **teardown**(`body`): `void`

Register a `teardown(fn)` body, run after every scenario with the context that
 `setup` returned.

## Parameters

### body

(`ctx`) => `void`

## Returns

`void`
